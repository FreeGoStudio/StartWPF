<Window x:Class="ToDo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDo"
        mc:Ignorable="d"
        Title="ToDo" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="AddButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="TextBoxFont" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--左侧菜单-->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <!--任务列表-->
            <ListBox Grid.Row="0" Margin="10" Background="#FFEEEEEE">
                <ListBoxItem Content="任务列表1" />
                <ListBoxItem Content="任务列表2" />
                <ListBoxItem Content="任务列表3" />
            </ListBox>
            <!--任务列表操作区-->
            <Button Grid.Row="1"  Width="180" Height="30" Style="{StaticResource AddButton}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="pack://application:,,,/Images/add.png" Width="20" Height="20" Margin="0,0,5,0"/>
                    <TextBlock Text="新建列表" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!--右侧内容-->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <!--任务列表-->
            <ListBox Grid.Row="0" Margin="10" Background="#FFEEEEEE">
                <ListBoxItem Content="任务1" />
                <ListBoxItem Content="任务2" />
                <ListBoxItem Content="任务3" />
            </ListBox>
            <!--任务操作区-->
            <TextBox Grid.Row="1"
                     Margin="10,5"
                     Style="{StaticResource TextBoxFont}"
                     PreviewKeyDown="TaskTextBox_PreviewKeyDown"/>
        </Grid>
    </Grid>
</Window>
